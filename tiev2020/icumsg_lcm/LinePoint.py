"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

class LinePoint(object):
    __slots__ = ["x", "y"]

    __typenames__ = ["float", "float"]

    __dimensions__ = [None, None]

    def __init__(self):
        self.x = 0.0
        self.y = 0.0

    def encode(self):
        buf = BytesIO()
        buf.write(LinePoint._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">ff", self.x, self.y))

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != LinePoint._get_packed_fingerprint():
            raise ValueError("Decode error")
        return LinePoint._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = LinePoint()
        self.x, self.y = struct.unpack(">ff", buf.read(8))
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if LinePoint in parents: return 0
        tmphash = (0xfdeae9e49158c64f) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff) + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if LinePoint._packed_fingerprint is None:
            LinePoint._packed_fingerprint = struct.pack(">Q", LinePoint._get_hash_recursive([]))
        return LinePoint._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)

