"""ZCM type definitions
This file automatically generated by zcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

class TrajectoryPoint(object):
    __slots__ = ["x", "y", "theta", "t", "k", "v", "a"]

    IS_LITTLE_ENDIAN = False;
    def __init__(self):
        self.x = 0.0
        self.y = 0.0
        self.theta = 0.0
        self.t = 0.0
        self.k = 0.0
        self.v = 0.0
        self.a = 0.0

    def encode(self):
        buf = BytesIO()
        buf.write(TrajectoryPoint._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">ddddddd", self.x, self.y, self.theta, self.t, self.k, self.v, self.a))

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != TrajectoryPoint._get_packed_fingerprint():
            raise ValueError("Decode error")
        return TrajectoryPoint._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = TrajectoryPoint()
        self.x, self.y, self.theta, self.t, self.k, self.v, self.a = struct.unpack(">ddddddd", buf.read(56))
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if TrajectoryPoint in parents: return 0
        tmphash = (0xfbd1acfe8b4b823) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff)  + ((tmphash>>63)&0x1)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if TrajectoryPoint._packed_fingerprint is None:
            TrajectoryPoint._packed_fingerprint = struct.pack(">Q", TrajectoryPoint._get_hash_recursive([]))
        return TrajectoryPoint._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)

